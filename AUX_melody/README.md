
#🎸 MIK32 Guitar Synth (DMA + DAC + Timer32)
Этот проект — простой синтезатор гитарных нот на микроконтроллере MIK32V2, который воспроизводит мелодии через DAC, используя DMA и Timer32.
Музыка задаётся в виде массива нот:
Note melody1[] = { {3, 2, 50}, {2, 2, 50}, {6, 0, 1}, {2, 2, 50}, {1, 0, 50}, ... }; 
Каждая нота описывается как:
string — номер струны (аналог гитары)
fret — номер лада (0..12)
duration — длительность ноты (условные единицы)
⚙️ Как работает проект
Проект генерирует звук следующим образом:
1) Генерация синусоиды
В начале работы создаётся таблица sine_table[], содержащая значения синусоиды:
uint16_t sine_table[DAC_BUF_SIZE]; 
Таблица заполняется функцией:
init_sine_table(); 
Синусоида создаётся с применением оконной функции Ханна, чтобы уменьшить щелчки и сделать звук более мягким.
2) Воспроизведение через DAC + DMA
Чтобы микроконтроллер не тратил процессорное время на постоянную отправку данных в DAC, используется DMA:
HAL_DMA_Start(&hdma_ch0, (void *)&sine_table, (void *)&hdac1.Instance_dac->VALUE, sizeof(sine_table) - 1); 
То есть:
DMA автоматически копирует данные из массива sine_table
напрямую в регистр DAC (VALUE)
без участия CPU
3) Частота ноты задаётся таймером Timer32
Скорость выдачи значений синусоиды (и значит частота звука) задаётся через Timer32.
Для каждой ноты пересчитывается значение Top:
uint32_t top = TIMER_CLK / (freq * DAC_BUF_SIZE); htimer32.Top = top; 
Таким образом:
чем меньше Top → тем быстрее таймер → тем выше частота звука
чем больше Top → тем ниже частота
4) Гитарная модель: струны и лады
В проекте задан массив частот открытых струн:
uint16_t eBGDAE[] = {330, 247, 196, 147, 110, 82, 0}; 
Это похоже на настройку гитары (E B G D A E).
Для получения частоты при нажатии на лад используется таблица коэффициентов:
fret_mul_q12[13] 
Формула:
freq = (base_string_freq * fret_mul_q12[fret]) / 1000; 
То есть лад увеличивает частоту примерно по музыкальному закону 2^(n/12).
5) Проигрывание мелодии
Основной цикл:
постоянно запускает DMA (если канал свободен)
считает таймер длительности текущей ноты
переключает ноту, когда время прошло
Код переключения:
note_timer++; if (note_timer >= melody1[note_index].duration * SPEED) { note_timer = 0; note_index++; if (note_index >= melody1_len) note_index = 0; StartNote(&melody1[note_index]); } 
🎵 Мелодии
В проекте есть две мелодии формата:
Note melody1[] = { {string, fret, duration}, ... }; 
Например:
{3, 2, 50} → 3-я струна, 2-й лад, длительность 50
{6, 0, 1} → пауза (струна 6 = 0)
🔊 Управление громкостью
Громкость задаётся через макрос:
#define VOLUME 10 
Диапазон: 0..100 (%)
🧠 Что такое SPEED
Макрос:
#define SPEED 1900 
Это множитель длительности нот. Чем больше значение, тем медленнее мелодия.
🛠️ Сборка (PlatformIO)
Проект собирается в PlatformIO с настройками:
оптимизация по размеру -Os
удаление неиспользуемого кода --gc-sections
Пример platformio.ini:
build_flags = -Os -ffunction-sections -fdata-sections -Wl,--gc-sections -lm 
Используется линковщик spifi, чтобы программа помещалась во Flash.
📌 Используемые технологии
MIK32V2 SDK
DAC
DMA
Timer32
синусоидальный wavetable-синтез
музыкальная шкала 12-TET (2^(n/12))
🚀 Идеи для улучшения
Добавить ADSR огибающую (атака/спад)
Добавить несколько голосов (полифония)
Реализовать реальные гитарные тембры (Karplus–Strong)
Добавить поддержку двух DAC каналов (стерео)
Сделать таблицу синуса меньше (256) и интерполяцию
📷 Пример структуры проекта
src/ main.c include/ melody.h setting.h ... platformio.ini README.md 
📜 Лицензия
Проект свободный, можно использовать для учебных и инженерных целей.
Если хочешь — я могу ещё добавить в README:
красивый баннер
схему подключения DAC/пинов
таблицу соответствия струн (string → нота)
описание формата Note из melody.h
